# homebrew-tap

Homebrew formulae for EIT UMD tools.

## Usage

```sh
brew tap eitumd/tap
```

## Available formulae

| Formula | Description |
|---------|-------------|
| `aws-oidc` | OIDC credential helper for AWS CLI using PKCE |

```sh
brew install aws-oidc
```
